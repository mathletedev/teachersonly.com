import { FC } from "react";

const Welcome: FC = () => {
	return (
		<div className="flex h-screen w-screen">
			<div className="text-grey m-auto">currently under development...</div>
		</div>
	);
};

export default Welcome;
